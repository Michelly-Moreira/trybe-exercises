/* function sum(a, b) {
  if (typeof a !== 'number' || typeof b !== 'number') {
    throw new Error('parameters must be numbers');
  }

  return a + b;
}
 */


/* ESCREVENDO TESTE EM JEST (implementação da função)
const sum = (a, b) => a + b;

module.exports = sum;    */ //exportando a função para o arquivo test.js